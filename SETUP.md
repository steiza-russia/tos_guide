# Настройка и запуск VuePress

## Быстрый старт

### Установка зависимостей

```bash
cd /Users/n.trofimov/Documents/steiza/Инструкции/site
npm install
```

### Режим разработки

Для локального просмотра с горячей перезагрузкой:

```bash
npm run docs:dev
```

Сайт будет доступен по адресу: `http://localhost:8080`

### Сборка для продакшн

Для создания статических HTML файлов:

```bash
npm run docs:build
```

Результат будет в папке `.vuepress/dist/`

---

## Автоматический деплой

### Вариант 1: GitHub Pages (рекомендуется) 🌟

📖 **Полная инструкция:** [DEPLOY-GITHUB-PAGES.md](./DEPLOY-GITHUB-PAGES.md)

**Быстрый старт:**
1. В настройках репозитория: **Settings → Pages → Source: GitHub Actions**
2. Push в `main` → автоматически соберется и задеплоится
3. Сайт будет доступен на `https://username.github.io/steiza/`

✅ Бесплатно, HTTPS, CDN, настройка за 5 минут

### Вариант 2: Собственный сервер с Nginx

📖 **Полная инструкция:** [DEPLOY.md](./DEPLOY.md)

**Быстрая версия:**
1. Настроить сервер (Nginx, SSH)
2. Добавить GitHub Secrets (SSH_PRIVATE_KEY, REMOTE_HOST, REMOTE_USER, REMOTE_PATH)
3. Push в `main` → автоматически соберется и задеплоится

✅ Полный контроль, custom домен, интеграция с инфраструктурой

## Структура проекта

```
site/
├── .vuepress/
│   ├── config.js           # Конфигурация VuePress
│   ├── styles/
│   │   ├── palette.css     # Цветовая схема
│   │   └── index.css       # Дополнительные стили
│   ├── public/
│   │   ├── .nojekyll       # Для GitHub Pages
│   │   └── images/         # Изображения для инструкций
│   └── dist/               # Собранный сайт (после build)
├── docs/                   # Папка с инструкциями
│   ├── README.md           # Главная страница раздела инструкций
│   └── Как добавить клиента в систему?.md
├── README.md               # Главная страница сайта
└── package.json            # Зависимости проекта
```

## Добавление новых инструкций

1. Создайте новый `.md` файл в папке `docs/`
2. Добавьте заголовок первого уровня `# Название`
3. Добавьте изображения в `.vuepress/public/images/`
4. Используйте синтаксис `![Описание](/images/название.png)`
5. Обновите sidebar в `.vuepress/config.js`
6. Добавьте ссылку в `docs/README.md`

Пример добавления в sidebar:

```js
sidebar: {
  '/docs/': [
    {
      text: 'Инструкции',
      children: [
        '/docs/Как добавить клиента в систему_',
        '/docs/Новая инструкция',  // <- добавьте здесь (без .md)
      ],
    },
  ],
},
```

---

## Кастомизация внешнего вида

📖 **Полная инструкция:** [CUSTOMIZATION.md](./CUSTOMIZATION.md)

### Изменение цветов

Редактировать `.vuepress/styles/palette.css`:

```css
:root {
  --c-brand: #3eaf7c;     /* Основной цвет */
  --c-brand-light: #4abf8a;
  --c-brand-hover: #2d8659;
}
```

### Дополнительные стили

Добавить в `.vuepress/styles/index.css`

Примеры готовых цветовых схем и настройки шрифтов - в [CUSTOMIZATION.md](./CUSTOMIZATION.md)

## Деплой

Папку `.vuepress/dist/` можно разместить на любом статическом хостинге:
- GitHub Pages
- Netlify
- Vercel
- Nginx/Apache сервер

